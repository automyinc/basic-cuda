package automy.basic_cuda;

module CUDA_Bridge {
	
	int device = -1;
	
	string cpu_domain;
	string cuda_domain;
	
	vector<string> upload;
	vector<string> download;
	
	int interval_ms = 100;
	
	bool auto_download = false;
	
	
	void handle(automy.basic.ImageFrame sample);
	
}
